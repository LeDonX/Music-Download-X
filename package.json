{
  "name": "music-download-x",
  "version": "1.0.0",
  "description": "Music Download X - High Quality Music Downloader built with Vite",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"wrangler pages dev --ip 0.0.0.0 --proxy http://localhost:5173\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "vite": "^5.2.0",
    "wrangler": "^3.57.1"
  }
}
